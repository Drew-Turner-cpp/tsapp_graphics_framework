import tsapp as ts
import framework as f


window = ts.GraphicsWindow()  # Create GraphicsWindow


# Main Loop
while window.is_running:  # Keep window open until user closes it
    window.finish_frame()
